[project]
name = "hdf5histviewer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "h5py",
    "numpy",
    "prompt_toolkit",
]

[dependency-groups]
dev = [
    "pytest",
]
